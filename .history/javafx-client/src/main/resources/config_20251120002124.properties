# Client Configuration
# ⚠️ QUAN TRỌNG: Thay đổi IP addresses theo máy thực tế của bạn
# Chạy script: scripts/get-ip.sh (Linux/macOS) hoặc scripts/get-ip.bat (Windows) để lấy IP
# Sau đó chạy: scripts/setup-network.sh hoặc scripts/setup-network.bat để tự động config

tracker.url=http://192.168.1.52:8081
origin.server.url=http://192.168.1.18:8080
client.name=win-Client-1

# Network Configuration
client.bind.address=0.0.0.0
peer.server.port=6881

# Logging
log.level=INFO
log.show.ip=true
log.show.flow=true

