# Set server port to 8080 for HTTP communication
server.port=8080

# SSL is disabled for simplicity in the demo environment.
# To enable HTTPS, uncomment the following lines, set the port to 8443, and ensure the keystore exists.
#
# server.ssl.enabled=true
# server.ssl.key-store=classpath:keystore.p12
# server.ssl.key-store-password=changeit
# server.ssl.key-store-type=PKCS12
# server.ssl.key-alias=origin-server
# server.port=8443
